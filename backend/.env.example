# Server
PORT=3000

# MongoDB – use full URI and/or host + port
# Option 1: Full URI (recommended for Atlas or custom connection string)
MONGODB_URI=mongodb://localhost:27017/freevpn

# Option 2: Build URI from parts (uncomment and set if not using MONGODB_URI)
# MONGODB_HOST=localhost
# MONGODB_PORT=27017
# MONGODB_DATABASE=freevpn
# MONGODB_USER=
# MONGODB_PASSWORD=

# NODE_ENV=development
